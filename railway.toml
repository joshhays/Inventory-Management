# Railway deployment config
# https://docs.railway.app/reference/config-as-code

[build]
builder = "nixpacks"

[deploy]
startCommand = "prisma db push && node src/server.js"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
