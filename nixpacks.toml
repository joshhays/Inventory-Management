# Skip canvas optional deps to avoid native build; PDF previews show placeholder
[phases.install]
cmds = ['npm install --omit=optional']

[phases.build]
cmds = ['npx prisma generate', 'npm run build']

# prisma migrate deploy applies migrations (including AdminGroup); db push for schema-only sync
[start]
cmd = "prisma migrate deploy && node src/server.js"
