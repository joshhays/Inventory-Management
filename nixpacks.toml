# Nixpacks config for Railway
# Skip optional deps (canvas) to avoid native build; PDF preview falls back to raw PDF
[phases.install]
cmds = ["npm install --omit=optional"]

[phases.build]
cmds = ["npm run build"]
