[phases.setup]
nixPkgs = ['...', 'python3', 'pkg-config', 'pixman', 'cairo', 'pango', 'libpng', 'jpeg', 'giflib', 'librsvg']

[phases.install]
# We remove --omit=optional to ensure canvas builds
cmds = ['npm install']

[phases.build]
cmds = ['npm run build']
