[phases.setup]
nixPkgs = ['...', 'python3', 'pkg-config', 'pixman', 'cairo', 'pango', 'libpng', 'libjpeg', 'giflib', 'librsvg']

[phases.install]
cmds = ['npm install']

[phases.build]
cmds = ['npm run build']
